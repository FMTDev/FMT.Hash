//using System.IO.Hashing;

namespace FMT.Hash
{
    public static class CRC32Hash
    {
        //public uint HashString(string s)
        //{
        //    s = s.ToLowerInvariant();
        //    byte[] data = Encoding.ASCII.GetBytes(key.ToLowerInvariant());
        //    uint crc = Crc32.Hash(data);
        //    return crc;
        //}
    }
}
